---
import { getCollection } from 'astro:content';
import Button from '../../components/Button.astro';
import PostPreview from '../../components/PostPreview.astro';
import Profile from '../../components/Profile.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { sortItemsByDateDesc } from '../../utils/data-utils';

const posts = (await getCollection('blog')).filter(({ data }) => data.lang === 'zh').sort(sortItemsByDateDesc).slice(0, 5);
---

<BaseLayout title="首页" description="Resery：Android 与 TEE 安全研究，以及最近文章。" locale="zh" alternateUrl="/Blog/" showHeader={false} wide>
    <Profile locale="zh">
        <section class="academic-section home-writing" aria-labelledby="latest-writing">
            <h2 id="latest-writing">最近文章</h2>
            <div class="post-list">
                {posts.map((post) => <PostPreview post={post} locale="zh" headingLevel="h3" />)}
            </div>
            <Button href="/Blog/zh/blog/">全部文章</Button>
        </section>
    </Profile>
</BaseLayout>
